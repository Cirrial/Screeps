module.exports = function() {
    var designs = {
        builder: {
            body: [WORK, WORK, CARRY, MOVE]
        },
        harvester: {
            body: [WORK, CARRY, MOVE]
        },
        guard: {
            body: [ATTACK, MOVE, MOVE]
        }
    };

    return designs;
};

